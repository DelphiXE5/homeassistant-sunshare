# Secrets / session state — never commit
.sunshare_token.json
.sunshare_credentials.json
*.keystore

# Local tooling
node_modules/
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Local Claude Code project settings — machine-specific, not for sharing
.claude/

# Large / sensitive reverse-engineering artifacts (kept locally only — see
# .reverse_engieneering_data/README.md). Also keeping the general *.pcap
# pattern as a safety net in case a future capture lands outside that folder.
.reverse_engieneering_data/
*.pcap

# Patched/signed APKs produced during the MITM experiment
apk_orig/
resigned_splits/
final_signed/
*.apk
